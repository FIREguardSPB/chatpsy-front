# Chat Psychology Analyzer

Анализ переписки с помощью ИИ: психологический портрет участников, динамика отношений и рекомендации.

## Возможности

- 🧠 **Анализ переписки** — получите психологический разбор общения с помощью ИИ
- 📊 **Мета-информация** — быстрая статистика по переписке без использования ИИ
- 🔐 **Конфиденциальность** — анонимизация данных перед отправкой на сервер
- 💬 **Поддержка форматов** — экспорт из WhatsApp (.txt) и Telegram (.html)
- 🗂️ **Поддержка ZIP-архивов** — возможность загрузки ZIP-архивов с TXT-файлами чата
- 📅 **Фильтрация по датам** — анализ только выбранного периода переписки
- 🌐 **Мультиязычность** — интерфейс на русском языке

## Поддерживаемые форматы

- `.txt` — экспорт из WhatsApp
- `.html` — экспорт из Telegram
- `.zip` — архивы с TXT-файлами чата

## Установка и запуск

### Фронтенд (React + TypeScript)

```bash
cd chat-psy-analyzer
npm install
npm run dev
```

### Бэкенд (FastAPI + Python)

```bash
cd backend
pip install -r requirements.txt
uvicorn app.main:app --reload
```

## Использование

1. Экспортируйте чат из WhatsApp или Telegram
2. Загрузите файл(ы) в приложение
3. При необходимости укажите диапазон дат для анализа
4. Нажмите кнопку "Анализ" и дождитесь результата

### Работа с ZIP-архивами

Вы можете загружать ZIP-архивы, содержащие TXT-файлы чата. Сервис автоматически:
- Распакует архив
- Проверит, что все файлы имеют расширение .txt
- Объединит содержимое всех файлов для анализа
- Отобразит ошибку, если в архиве есть файлы других типов

## API Endpoints

- `POST /chat_meta` — быстрая статистика по переписке
- `POST /analyze_chat` — полный анализ переписки с ИИ
- `POST /analyze_zip` — обработка ZIP-архивов с TXT-файлами

## Лицензия

MIT