module.exports = {
  // Use single quotes instead of double quotes
  singleQuote: true,
  // Print trailing commas wherever possible when multi-line
  trailingComma: 'all',
  // Use 2 spaces for indentation
  tabWidth: 2,
  // Use spaces instead of tabs
  useTabs: false,
  // Add a semicolon at the end of every statement
  semi: true,
  // Print spaces between brackets in object literals
  bracketSpacing: true,
  // Include parentheses around a sole arrow function parameter
  arrowParens: 'always',
  // Line width before breaking
  printWidth: 100,
  // Line endings
  endOfLine: 'auto',
  // JSX
  jsxSingleQuote: true,
  // Indent JSX with 2 spaces
  jsxBracketSameLine: false,
  // Format embedded code
  embeddedLanguageFormatting: 'auto',
  // HTML whitespace sensitivity
  htmlWhitespaceSensitivity: 'css',
  // Require a 'prettier' from ESLint config
  requirePragma: false,
  // Insert @format pragma
  insertPragma: false,
  // Wrap prose if it exceeds the print width
  proseWrap: 'preserve',
  // Put > of multi-line JSX elements at the end of the last line
  bracketSameLine: false,
  // Use single quotes in JSX
  singleAttributePerLine: false,
};
